body {
  margin: 0;
  font-family: "Inter", system-ui, sans-serif;
  background:
    radial-gradient(circle at top left, #fbbf24 0, rgba(15, 23, 42, 1) 45%),
    radial-gradient(circle at bottom right, #2563eb 0, #020617 55%);
  color: #f9fafb;
}

.overlay {
  min-height: 100vh;
  background: radial-gradient(circle at top, rgba(15, 23, 42, 0.85), rgba(3, 7, 18, 0.98));
}

.wrap {
  max-width: 1120px;
  margin: 0 auto;
  padding: 2.6rem 1.4rem 3.4rem;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 3.3rem;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

.logo-img {
  height: 40px;
  width: auto;
}

.logo-text {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.16em;
  color: #fef3c7;
}

nav a {
  text-decoration: none;
  font-size: 0.8rem;
  color: #e5e7eb;
  margin-left: 1.4rem;
  text-transform: uppercase;
  letter-spacing: 0.18em;
}

nav a:hover {
  color: #facc15;
}

.hero {
  display: grid;
  gap: 2.4rem;
}

@media (min-width: 900px) {
  .hero {
    grid-template-columns: minmax(0, 1.25fr) minmax(0, 1.1fr);
    align-items: center;
  }
}

.eyebrow {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.22em;
  color: #facc15;
  margin-bottom: 0.7rem;
}

h1 {
  font-family: "DM Serif Display", serif;
  font-size: clamp(2.3rem, 4.3vw, 3.3rem);
  margin-bottom: 1rem;
}

.lede {
  color: #e5e7eb;
  max-width: 32rem;
  font-size: 0.96rem;
}

.hero-board {
  border-radius: 1.6rem;
  border: 1px solid rgba(15, 23, 42, 0.95);
  background:
    radial-gradient(circle at 0 0, rgba(251, 191, 36, 0.35), rgba(15, 23, 42, 0.96)),
    radial-gradient(circle at 100% 100%, rgba(37, 99, 235, 0.35), rgba(15, 23, 42, 1));
  padding: 1.5rem 1.4rem;
  box-shadow: 0 22px 48px rgba(15, 23, 42, 0.95);
  font-size: 0.9rem;
}

.board-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1rem;
}

.board-title {
  text-transform: uppercase;
  letter-spacing: 0.22em;
  font-size: 0.75rem;
  color: #fef3c7;
}

.badge {
  font-size: 0.75rem;
  border-radius: 999px;
  padding: 0.18rem 0.7rem;
  border: 1px solid rgba(248, 250, 252, 0.6);
  text-transform: uppercase;
  letter-spacing: 0.15em;
  background: rgba(15, 23, 42, 0.85);
}

.metrics {
  display: grid;
  gap: 0.7rem;
}

@media (min-width: 600px) {
  .metrics {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

.metric {
  border-radius: 1rem;
  border: 1px solid rgba(15, 23, 42, 0.95);
  background: rgba(15, 23, 42, 0.9);
  padding: 0.7rem 0.8rem;
}

.metric-label {
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.16em;
  color: #e5e7eb;
  opacity: 0.8;
  margin-bottom: 0.2rem;
}

.metric-value {
  font-size: 1.05rem;
  font-weight: 600;
}

.metric-note {
  font-size: 0.76rem;
  color: #e5e7eb;
  opacity: 0.85;
  margin-top: 0.1rem;
}

section {
  margin-top: 3.1rem;
}

h2 {
  font-family: "DM Serif Display", serif;
  font-size: 1.6rem;
  margin-bottom: 1rem;
}

.two-col {
  display: grid;
  gap: 1.8rem;
}

@media (min-width: 900px) {
  .two-col {
    grid-template-columns: minmax(0, 1.3fr) minmax(0, 1.1fr);
  }
}

.list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.list li {
  display: flex;
  gap: 0.6rem;
  margin-bottom: 0.9rem;
  font-size: 0.9rem;
}

.icon {
  width: 18px;
  height: 18px;
  border-radius: 999px;
  border: 1px solid rgba(252, 211, 77, 0.9);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.7rem;
}

.panel {
  border-radius: 1.3rem;
  border: 1px solid rgba(15, 23, 42, 0.95);
  background: rgba(15, 23, 42, 0.9);
  padding: 1.1rem 1rem;
  font-size: 0.86rem;
  color: #e5e7eb;
}

.panel-copy {
  margin-top: 0;
  margin-bottom: 0.75rem;
}

footer {
  border-top: 1px solid rgba(15, 23, 42, 0.9);
  margin-top: 3.1rem;
  padding-top: 1.2rem;
  font-size: 0.76rem;
  color: #e5e7eb;
  opacity: 0.85;
}



body {
  margin: 0;
  font-family: "Inter", system-ui, sans-serif;
  background: #0b1120;
  color: #e5e7eb;
}

.wrap {
  max-width: 960px;
  margin: 0 auto;
  padding: 2.4rem 1.3rem 3rem;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 3rem;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.6rem;
}

.logo-img {
  height: 36px;
  width: auto;
}

.logo-tag {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: #9ca3af;
}

nav a {
  text-decoration: none;
  font-size: 0.82rem;
  color: #9ca3af;
  margin-left: 1.3rem;
  text-transform: uppercase;
  letter-spacing: 0.16em;
}

nav a:hover {
  color: #e5e7eb;
}

.hero {
  margin-bottom: 3rem;
}

h1 {
  font-family: "DM Serif Display", serif;
  font-size: clamp(2.1rem, 4vw, 2.7rem);
  margin-bottom: 0.9rem;
}

.lede {
  color: #9ca3af;
  max-width: 30rem;
  font-size: 0.95rem;
  margin-bottom: 1.6rem;
}

.hero-ctas {
  display: flex;
  flex-wrap: wrap;
  gap: 0.7rem;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.65rem 1.5rem;
  border-radius: 999px;
  border: 1px solid #60a5fa;
  background: #1d4ed8;
  color: #f9fafb;
  font-size: 0.8rem;
  text-decoration: none;
  text-transform: uppercase;
  letter-spacing: 0.18em;
}

.btn-secondary {
  background: transparent;
  border-color: #4b5563;
  color: #e5e7eb;
}

section {
  margin-bottom: 3rem;
}

h2 {
  font-family: "DM Serif Display", serif;
  font-size: 1.5rem;
  margin-bottom: 1rem;
}

.services-grid {
  display: grid;
  gap: 1.2rem;
}

@media (min-width: 700px) {
  .services-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

.card {
  border-radius: 1rem;
  border: 1px solid #1f2937;
  padding: 1.1rem 1rem;
  background: radial-gradient(circle at 0 0, rgba(96, 165, 250, 0.16), #020617);
  font-size: 0.9rem;
}

.card h3 {
  margin-top: 0;
  margin-bottom: 0.4rem;
  font-size: 1rem;
}

.copy {
  color: #9ca3af;
  max-width: 32rem;
  font-size: 0.95rem;
}

footer {
  border-top: 1px solid #111827;
  padding-top: 1.1rem;
  font-size: 0.75rem;
  color: #6b7280;
}
